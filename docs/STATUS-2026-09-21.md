# ClawMaster status report — 2026-09-21

English | [中文](STATUS-2026-09-21.zh.md)

> **Final update (2026-09-21 evening)**: the original `NSIETeam/ClawMaster` repository has been confirmed deleted (cleaned up after archiving).
> This repository (NSIETeam/ClawMaster, formerly ClawMaster-Desktop, renamed 2026-09-21) is now the sole remote. Local main has switched to the DSH workline;
> the old product line is preserved in tag `legacy/main-pre-dsh` and branch `archive/legacy-main-pre-dsh` (if pushed).
> Enterprise plugin skill probes pass **10/10**; all 93 sessions in the session store are healthy; launchd self-heal every 30 minutes is live.

## Current status overview

| Item | Status |
|---|---|
| Runtime | dsh 0.1.5-rc.2 (harness-versions/72da6c767414dd30, Sep 19 provision) |
| Desktop | ClawMaster.app 0.0.1beta (version reset 2026-09-21; repository source version, no matching release yet; the latest release remains desktop-v0.2.3) |
| Session store | All 93 sessions pass validation (session-doctor 2026-09-21 13:5x) |
| Skills | 106 (71 marketplace plugin skills + 27 enterprise management + 3 anthropics official + 28 lark in ~/.agents/skills) |
| Proactive skill loading | Verified working (user-layer patch cordis.patch.yml + skill-discipline system-prompt section) |

## Fixed this round

1. **Missing role in the session store** (history loading failed wholesale): 21 sessions / 93 events patched with id/role/source.
   - Script: `~/.dsh/scripts/repair-session-roles.mjs` (role-specific)
   - Script: `~/.dsh/scripts/session-doctor.mjs` (full-rule validation + repair + quarantine, the "one session = one plugin" strategy)
   - launchd self-heal: `com.clawmaster.session-repair` runs session-doctor every 30 minutes
2. **Proactive skill loading**: the model no longer waits for the user to name a skill; a matching task loads the `skill` tool automatically.
   - Injection point: `~/.dsh/cordis.patch.yml` (user layer; **the app does not rewrite it**)
   - ⚠️ `~/.dsh/desktop-overlay/cordis.yml` is rewritten back to the template by the app at every boot; do not put custom plugins there
3. **Skill frontmatter**: normalized descriptions + Chinese trigger phrases for the 27 enterprise-management skills (YAML parse failures previously dropped them silently)
4. **WeChat pipeline**: INTERNAL_UNKNOWN (tool dispatcher missing) resolved with environment cleanup + session reset

## Known issues / TODO

- [ ] The runtime `core/session/lib/index.js` "missing-role self-heal" patch is rolled back by the integrity restore at 08:40/boot;
      corrupted disk events may recur → launchd session-doctor every 30 minutes as the safety net.
      **Root fix**: upgrade the dsh kernel (repo HEAD already contains today's fixes).
- [ ] `harness-core-20260919.tar.gz` (sha 0256..., downloaded 07:19) is an unofficial artifact (unrecognized by repo/updater),
      not adopted. The official latest remains desktop-v0.2.3.
- [ ] 4 pure-MCP plugins (hexin/wind/tianyancha/finance-search) are ZCode-hosted HTTP MCP (JWT auth);
      the dsh MCP bridge supports stdio only → install natively from the ClawMaster plugin marketplace.
- [x] ~~The original NSIETeam/ClawMaster repository~~ deleted (2026-09-21); the snapshot branch lives in this repository: `archive/clawmaster-wip-20260919`
- [ ] Repository-root `packages/core/src`, `packages/server`, `packages/desktop`, `clawmaster-native/`,
      `deployment/`, enterprise release scripts, `tauri-preview.yml` etc., 604 files, snapshotted to WIP branch
      `codex/dsh-desktop-wip-20260919` (604 files / 284k lines).
      Missing files (native/sqlcipher-tauri, tauri-node-runtime.yml, browserPreviewBridge.ts, css.d.ts,
      security/e2ee-release-status.json, sqlcipher-native.yml) must be synced from the Windows build machine or have their references removed.
- [ ] `packages/bundle/web-app/tests/browser-open.spec.ts` 400 regression (tracked code, reproduces in isolation)
- [x] ~~`packages/server/src/server.ts:4703` hardcoded appVersion drift~~ resolved with the 2026-09-21 version reset: appVersion/updateCheck now return 0.0.1beta, consistent with desktop 0.0.1-beta
- [ ] `scripts/tests/*.test.js` gate tests are in no vitest include (orphan tests, CI never runs them)
- [ ] lint: all 64k style errors come from the three untracked snapshot directories (oxlint --fix can clear them)

## Key runbooks

- Corrupt-session repair: `node ~/.dsh/scripts/session-doctor.mjs` (see `~/ClawMaster/memory/2026-09-21-session-role-repair-runbook.md`)
- Session directories must **never be renamed** (header id/cwd are strongly validated against the path)
- Files under `harness-versions/` are read-only with integrity restore; patching them is futile; user-layer config goes in `~/.dsh/cordis.patch.yml`
- CI/release: the process watching the CI run already exists; latest release = desktop-v0.2.3

## Postscript (2026-09-21 14:4x)

- Enterprise plugin probe tests pass **10/10** (one unnamed task per plugin; the model proactively loaded the correct skill every time),
  report at `~/ClawMaster/memory/2026-09-21-enterprise-plugin-probe-report.md`.
- Local main switched to the DSH workline via replacement (old line tag: `legacy/main-pre-dsh`),
  GitHub backup branch: ClawMaster-Desktop's `clawmaster/main`.
- The skill-discipline plugin's durable mount point moved to the user layer `~/.dsh/cordis.patch.yml` (the overlay gets rewritten by the app).
