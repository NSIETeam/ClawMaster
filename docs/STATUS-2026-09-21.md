# ClawMaster 状态报告 — 2026-09-21

> **最终更新（2026-09-21 晚）**：原 `NSIETeam/ClawMaster` 仓库已确认删除（归档后清理）。
> 本仓库（ClawMaster-Desktop）现在是唯一远端。本地 main 已切换至 DSH 工作线，
> 旧产品线保留在 tag `legacy/main-pre-dsh` 与分支 `archive/legacy-main-pre-dsh`（如已推送）。
> 企业插件技能探针 **10/10 通过**；会话库 93 个会话全部健康；launchd 每 30 分钟自愈已上线。


## 当前状态总览

| 项 | 状态 |
|---|---|
| 运行时 | dsh 0.1.5-rc.2（harness-versions/72da6c767414dd30，Sep 19 provision） |
| 桌面端 | ClawMaster.app 0.2.3（= 最新官方 release desktop-v0.2.3；9/19 的 CI run 35422531372 失败，无更新版） |
| 会话库 | 93 个会话全部通过校验（session-doctor 2026-09-21 13:5x） |
| 技能 | 106 个（71 市场插件技能 + 27 企业管理 + 3 anthropics 官方 + lark 28 在 ~/.agents/skills） |
| 主动调技能 | 已验证生效（用户层补丁 cordis.patch.yml + 技能纪律系统段） |

## 本次已修复

1. **会话库 role 缺失**（历史加载全挂）：21 会话/93 事件补齐 id/role/source。
   - 脚本：`~/.dsh/scripts/repair-session-roles.mjs`（role 专项）
   - 脚本：`~/.dsh/scripts/session-doctor.mjs`（全规则校验+修复+隔离，"每会话=独立插件"策略）
   - launchd 自愈：`com.clawmaster.session-repair` 每 30 分钟自动跑 session-doctor
2. **技能主动调取**：模型不再等用户点名技能，任务匹配即自动调 `skill` 工具。
   - 注入点：`~/.dsh/cordis.patch.yml`（用户层，**应用不会重写它**）
   - ⚠️ `~/.dsh/desktop-overlay/cordis.yml` 会被应用启动时重写回模板，别把自定义插件放那里
3. **技能 frontmatter**：27 个企业管理技能的 description 规范化 + 中文触发词（此前 YAML 解析失败被静默丢弃）
4. **微信链路**：INTERNAL_UNKNOWN（工具调度器缺失）已随环境清理+会话重置解决

## 已知问题 / 待办

- [ ] 运行时 `core/session/lib/index.js` 的"缺 role 自愈"补丁会被 08:40/启动时的完整性恢复回滚，
      磁盘坏事件可能再产生 → launchd session-doctor 每 30 分钟兜底清理。
      **根治**：升级 dsh 内核（repo HEAD 已含 today's fixes）。
- [ ] `harness-core-20260919.tar.gz`（sha 0256...，07:19 下载）为非官方产物（repo/updater 均不识别），
      未采用。官方最新仍为 desktop-v0.2.3。
- [ ] 4 个纯 MCP 插件（hexin/wind/tianyancha/finance-search）为 ZCode 托管 HTTP MCP（JWT 鉴权），
      dsh MCP 桥仅支持 stdio → 建议在 ClawMaster 插件市场原生安装。
- [x] ~~原 NSIETeam/ClawMaster 仓库~~ 已删除（2026-09-21），快照分支在本仓库：`archive/clawmaster-wip-20260919`
- [ ] 仓库根 `packages/core/src`、`packages/server`、`packages/desktop`、`clawmaster-native/`、
      `deployment/`、企业发布脚本、`tauri-preview.yml` 等 604 文件已快照至 WIP 分支
      `codex/dsh-desktop-wip-20260919`（604 文件/28.4 万行）。
      缺失文件（native/sqlcipher-tauri、tauri-node-runtime.yml、browserPreviewBridge.ts、css.d.ts、
      security/e2ee-release-status.json、sqlcipher-native.yml）需从 Windows 构建机同步或摘除引用。
- [ ] `packages/bundle/web-app/tests/browser-open.spec.ts` 400 回归（tracked 代码，单跑复现）
- [ ] `packages/server/src/server.ts:4703` appVersion 硬编码 0.0.2beta ≠ 0.1.5-rc.2（快照分支待修）
- [ ] `scripts/tests/*.test.js` 门禁测试不在任何 vitest include（孤儿测试，CI 不跑）
- [ ] lint：6.4 万风格错误全部来自快照的三个未跟踪目录（oxlint --fix 可清）

## 关键操作手册

- 会话损坏修复：`node ~/.dsh/scripts/session-doctor.mjs`（详见 `~/ClawMaster/memory/2026-09-21-session-role-repair-runbook.md`）
- 会话目录**禁止改名**（头部 id/cwd 与目录强一致校验）
- `harness-versions/` 内文件只读且有完整性恢复，打补丁徒劳；用户层配置放 `~/.dsh/cordis.patch.yml`
- CI/发布：watch CI run 的进程已存在；最新 release = desktop-v0.2.3

## 附言（2026-09-21 14:4x）

- 企业插件探针测试 **10/10 通过**（每插件一个不点名任务，模型均主动调用正确 skill），
  报告见 `~/ClawMaster/memory/2026-09-21-enterprise-plugin-probe-report.md`。
- 本地 main 已采用替换式切换至 DSH 工作线（旧线 tag: `legacy/main-pre-dsh`），
  GitHub 备份分支：ClawMaster-Desktop 的 `clawmaster/main`。
- 技能纪律插件持久挂载点已迁至用户层 `~/.dsh/cordis.patch.yml`（overlay 会被应用重写）。
