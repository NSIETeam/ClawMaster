# 园区服务上线待办

> 状态：**P0-1 多人待办抢单与退回已完成**（internal@a3a6de4，issue #246 已关闭）；**P0-2 通知升级顺序已完成**（站内+飞书→5 分钟未读→短信，服务器重启后继续有效）。此文件随下一次代码同步后供 Felix 和所有开发成员共同查看。

## P0：多人待办的抢单与退回 ✅ 已完成

- 维修工作人员、园区客服人员都允许配置多名。
- 同一申请会进入符合职责标签的所有人员待办池。
- 第一名点击“接单并处理”的人员获得工单，服务器必须用原子更新保证只有一人抢单成功；其他人立即看到“已由某某接单”，不能继续处理。
- 工作人员选择“暂时没空”时，工单退回公共待办池，可由其他符合条件的人员接单。
- 数据库需要记录实际处理人、接单时间、退回时间和退回原因；接口冲突应返回明确状态，客户端同步刷新。
- 必须覆盖双设备同时接单、接单后退回、退回后重新接单、已完成工单不可重抢的自动化测试。

> 实现：`accept` 原子条件更新 + `release` 退回动作；`it_tickets` 新增处理人/退回列，`ticket_events` 增加 `release`；自动化测试覆盖 100 次并发唯一抢单、退回再抢、已完成不可重抢等场景。

## P0：通知升级顺序 ✅ 已完成

- 新申请、办理回复和办理结果：Otto 站内提醒与飞书立即发送。
- Otto 提醒发出 5 分钟后仍没有已读回执，才发送短信；已读则取消短信。
- 延时任务需要在服务器重启后继续有效，不能只依赖内存计时器。
- 每个渠道保留发送状态、失败原因和重试次数，管理员可以排查；普通用户不能看到手机号、飞书 open_id 或通知日志。
- 短信与飞书失败不能阻断工单创建，失败任务进入可重试队列。
- 必须覆盖 5 分钟内已读不发短信、超时未读发送短信、重复任务不重复发送、服务器重启后继续执行的自动化测试。

> 实现：`ticket_events`/`ticket_notifications`/`ticket_notification_tasks` 三表；站内提醒立即落已读回执，飞书即时发送失败进可重试任务队列；短信按 5 分钟（可配置）调度升级任务，`processTicketNotificationTasks` 在处理前校验已读回执，已读即取消、未读才发送，任务状态持久化（pending/sent/cancelled/failed）重启后续跑；`markTicketRead` 原子取消该收件人的待发短信任务。自动化测试覆盖 5 分钟内已读不发短信、超时未读发送、重复任务不重复发送、重启后继续执行等场景。

## 上线验收

- P0-1（抢单）与 P0-2（通知升级顺序）均已完成。
- P0-1 验收：使用两台 Otto 和两个工作人员账号做一次端到端验收（并发抢单/退回/再抢），再打正式安装包。
- P0-2 验收：两台 Otto 上各用一个工作人员账号，验证新申请/回复/结果 5 分钟内已读不发短信、超时未读收到短信、服务器重启后待发短信仍按原计划发送；管理员可查看各渠道状态/失败原因/重试次数，普通用户不可见手机号、飞书 open_id 与通知日志。
