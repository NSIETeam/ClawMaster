# Register the built-in RPA tools. Native inspection starts only when a tool
# executes; wechat_read requires one-time user approval before native access.
- insert:
    - id: clawmaster-rpa
      name: '@clawmaster/dsh-rpa'
