location = /updates/clawmaster/v2/latest.json {
    alias /var/lib/clawmaster-updates-v2/public/latest.json;
    default_type application/json;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/v2/versions/ {
    alias /var/lib/clawmaster-updates-v2/public/versions/;
    autoindex off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location = /updates/clawmaster/latest.json {
    alias /var/lib/clawmaster-updates/public/latest.json;
    default_type application/json;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/versions/ {
    alias /var/lib/clawmaster-updates/public/versions/;
    autoindex off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location = /updates/clawmaster/components/catalog.json {
    alias /var/lib/clawmaster-updates/components/current/catalog.json;
    default_type application/json;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location = /updates/clawmaster/components/catalog.json.sig {
    alias /var/lib/clawmaster-updates/components/current/catalog.json.sig;
    default_type text/plain;
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/components/artifacts/ {
    alias /var/lib/clawmaster-updates/components/artifacts/;
    autoindex off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/components/installers/ {
    alias /var/lib/clawmaster-updates/components/installers/;
    default_type application/javascript;
    autoindex off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/kits/ {
    alias /var/lib/clawmaster-updates/kits/;
    autoindex off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    add_header X-Content-Type-Options nosniff always;
    limit_except GET { deny all; }
}

location ^~ /updates/clawmaster/ {
    return 404;
}
