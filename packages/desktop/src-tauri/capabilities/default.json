{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the trusted main ClawMaster webview. External platform child webviews intentionally match no capability and cannot access IPC.",
  "webviews": ["main"],
  "permissions": ["core:default"]
}
