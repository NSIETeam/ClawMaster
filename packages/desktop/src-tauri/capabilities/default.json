{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the main ClawMaster window. System access is exposed only through validated Rust commands; raw plugin commands are intentionally not granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
