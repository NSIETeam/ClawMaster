{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the main ClawMaster window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
