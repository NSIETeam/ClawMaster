import { mkdtemp, readFile, rm } from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import { describe, expect, it } from 'vitest'; import { dueMeetingActions, registerMeetingActions } from './meeting-actions.js';
describe('meeting action tracking', () => { it('registers structured actions and selects due open/doing tasks', async () => { const dir = await mkdtemp(path.join(os.tmpdir(), 'meeting-actions-')); const file = path.join(dir, 'store.json'); try { await registerMeetingActions(file, '周会', [{ task: '交付方案', assignee: '小明', due: '2026-08-13' }, { task: '已完成', assignee: '小红', due: '2026-08-12', status: 'done' }]); const due = await dueMeetingActions(file, '2026-08-13'); expect(due).toHaveLength(1); expect(due[0]).toMatchObject({ task: '交付方案', assignee: '小明', status: 'open', source_meeting: '周会' }); expect(JSON.parse(await readFile(file, 'utf8')).actionItems).toHaveLength(2); } finally { await rm(dir, { recursive: true, force: true }); } }); it('rejects action items without an accountable owner/date', async () => { await expect(registerMeetingActions('unused', '周会', [{ task: '任务', assignee: '', due: '待确认' }])).rejects.toThrow('YYYY-MM-DD'); }); });
